#include <cmath>
#include <iostream>

// using namespace std;
int main() {
    float f;

    f = .5;
    std::cout << f <<" "<< 0.3*pow(f,4) << std::endl;

    f = -.5;
    std::cout << f <<" "<< 0.3*pow(f,4) << std::endl;

    f = 3;
    std::cout << f << " "<< 0.3*pow(f,4) << std::endl;

}
